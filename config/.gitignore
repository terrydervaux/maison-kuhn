# You can whitelist files/folders with !, these will not be ignored.
!*.yaml
!.gitignore
!*.md
*.log

# Ignore folders.
.storage
.cloud
.google.token

# Ensure these YAML files are ignored, otherwise your secret data/credentials will leak.
ip_bans.yaml
secrets.yaml
known_devices.yaml

# ignore DB files
*.db
*.db-wal
*.db-shm

# ignore docker-swag files
swag/